import DentalDetector from "@/components/DentalDetector";

export default function Home() {
  return (
    <main
      style={{
        minHeight: "100vh",
        padding: "24px 20px",
        maxWidth: "1100px",
        margin: "0 auto",
      }}
    >
      <DentalDetector />
    </main>
  );
}
